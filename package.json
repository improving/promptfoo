{
  "dependencies": {
    "ajv": "^8.20.0",
    "promptfoo": "^0.122.0"
  },
  "scripts": {
    "test": "promptfoo eval --no-cache --config ",
    "test:verbose": "promptfoo eval --no-cache --verbose --config $1 > test.log",
    "view": "promptfoo view"
  }
}
